
// The `cargo` crate users can still see and use the internal structure 
    // use cargo::kinds::PrimaryColor;
    // use cargo::utils::mix;

// Or they can use the more convenient structure as shown below
    // use cargo::PrimaryColor;
    // use cargo::mix;

fn main() {
    /*
     * By adding [profile.*] sections for any profile you want to customize,
     * you override any subset of the default settings
     *
     * Refer to https://doc.rust-lang.org/cargo/reference/profiles.html
     */
}
